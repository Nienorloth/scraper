from setuptools import setup

setup(
    name='prueba',
    version='0.0.1',
    description='Job searcher',
    py_modules=['scrape_jobs', 'print_all_jobs', 'prueba'],
    package_dir={'': 'src'},
)
